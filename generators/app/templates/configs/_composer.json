{
    "name": "<%= packagistVendor %>/<%= packagistPackage %>",
    "description": "<%= appDescription %>",
    "minimum-stability": "dev",
    "license": "<%= appLicense %>",
    "version": "<%= appVersion %>",
    "authors": [
        {
            "name": "<%= appAuthor %>",
            "email": "<%= appAuthorEmail %>"
        }
    ],

    "autoload":{
        "classmap": ["app/helpers", "app/models"]
    },

    "require":{
    },

    "requireDev":{
    },

    "suggest":{
    }
}
