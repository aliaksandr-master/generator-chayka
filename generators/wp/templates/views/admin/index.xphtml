<h2><%= pageTitle %></h2>

<div data-ng-app="chayka-options-form" data-ng-cloak>
    <div data-ng-controller="optionsForm" data-ng-init="namespace = '<%= phpNamespace.replace('//', '.') %>'">
        <form data-form-validator="validator" novalidate="novalidate">
        <!-- chayka: fields -->
        </form>
    </div>
</div>